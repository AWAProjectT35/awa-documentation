# Design Document

## API

### Without User Login

Return all restaurants

`GET /restaurants`

Return restaurant that matches `id`

`GET /restaurants/{id}`

Create a new user

`POST /users`

Log in user with `username`

`POST /users/{username}`
